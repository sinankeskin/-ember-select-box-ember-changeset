{{log 'Bar' (get this.fooChangeset 'bar')}}
<NativeSelectBox @value={{get this.fooChangeset 'bar'}} @onSelect={{this.handleSelectbar}} as |sb|>
  {{#each this.bars as |bar|}}
    <sb.Option @value={{bar}}>
      {{bar.name}}
    </sb.Option>
  {{/each}}
</NativeSelectBox>